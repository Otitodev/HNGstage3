# .env.example
# Environment configuration file for Country Currency & Exchange API
# Copy this file to .env and update the values for your environment

# ===== PostgreSQL Database Configuration =====
# PostgreSQL is the primary database system for this application
# All PostgreSQL connection parameters are configurable via environment variables

# PostgreSQL server hostname or IP address
# Default: localhost (for local development)
# Production: Use your PostgreSQL server hostname
POSTGRES_HOST=localhost

# PostgreSQL server port
# Default: 5432 (standard PostgreSQL port)
POSTGRES_PORT=5432

# PostgreSQL database name
# This database will be created if it doesn't exist
# Default: country_cache
POSTGRES_DB=country_cache

# PostgreSQL username for database connection
# Ensure this user has CREATE, READ, WRITE, DELETE permissions
# Default: postgres (superuser for development)
POSTGRES_USER=postgres

# PostgreSQL password for the specified user
# REQUIRED: Set a strong password for security
# Example: Use a mix of letters, numbers, and special characters
POSTGRES_PASSWORD=your_secure_password

# ===== Alternative Database Configuration =====
# Direct DATABASE_URL format (optional)
# If set, this will override the individual PostgreSQL variables above
# Format: postgresql+psycopg2://username:password@host:port/database
# DATABASE_URL=postgresql+psycopg2://postgres:your_secure_password@localhost:5432/country_cache

# ===== Application Configuration =====
# FastAPI application server settings

# Application host binding
# 0.0.0.0 allows external connections, 127.0.0.1 for localhost only
APP_HOST=0.0.0.0

# Application port number
# Default: 8000 (standard FastAPI development port)
APP_PORT=8080

# API refresh timeout in seconds
# Maximum time allowed for country data refresh operations
# Default: 20 seconds
REFRESH_TIMEOUT_SECONDS=20

